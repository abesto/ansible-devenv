taskd.certificate=~/.task/private.certificate.pem
taskd.key=~/.task/private.key.pem
taskd.ca=~/.task/ca.cert.pem
taskd.server={{ lookup('lastpass', 'taskwarrior/server', field='notes') }}
taskd.credentials={{ lookup('lastpass', 'taskwarrior/credentials', field='notes') }}
taskd.trust={{ lookup('lastpass', 'taskwarrior/trust', field='notes') }}